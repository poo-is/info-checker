#ifndef UTILS
	#define UTILS
	#include <iostream>
	#include <fstream>
	#include <algorithm>
	using namespace std;
	void citire_afisare(char *argv[]);
	void sortare (char *argv[]);
	void find(char *artv[]);
#endif